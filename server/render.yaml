services:
  - type: web
    name: sellify-api
    env: node
    buildCommand: npm install
    startCommand: node index.js
    envVars:
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: LICENSE_SECRET
        generateValue: true
    plan: free
